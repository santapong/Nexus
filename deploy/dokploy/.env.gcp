# NEXUS — GCP Compute Engine / Cloud Run Environment
# Platform: GCP (amd64)
# Recommended instance: e2-medium (2 vCPU, 4GB RAM) or larger
#
# Copy to .env and fill in real values before deploying

# ── Database ──
# For managed DB: use Cloud SQL connection string instead
# DATABASE_URL_OVERRIDE=postgresql+asyncpg://user:pass@/nexus?host=/cloudsql/project:region:instance
POSTGRES_DB=nexus
POSTGRES_USER=nexus
POSTGRES_PASSWORD=CHANGE_ME_use_a_strong_password

# ── Redis ──
# For managed Redis: use Memorystore connection
# REDIS_URL_OVERRIDE=redis://:password@memorystore-ip:6379
REDIS_PASSWORD=CHANGE_ME_redis_password
REDIS_MAXMEMORY=512mb

# ── LLM API Keys ──
ANTHROPIC_API_KEY=sk-ant-your-key-here
GOOGLE_API_KEY=your-google-api-key-here
OPENAI_API_KEY=
GROQ_API_KEY=
MISTRAL_API_KEY=

# ── Agent Model Assignments ──
MODEL_CEO=claude-sonnet-4-20250514
MODEL_ENGINEER=claude-sonnet-4-20250514
MODEL_ANALYST=gemini-2.0-flash
MODEL_WRITER=claude-haiku-4-5-20251001
MODEL_QA=claude-haiku-4-5-20251001
MODEL_PROMPT_CREATOR=claude-sonnet-4-20250514

# ── Cost Controls ──
DAILY_SPEND_LIMIT_USD=5.00
DEFAULT_TOKEN_BUDGET_PER_TASK=50000

# ── Auth ──
JWT_SECRET_KEY=CHANGE_ME_generate_a_strong_secret

# ── Application ──
LOG_LEVEL=INFO

# ── Ports ──
BACKEND_PORT=8000
FRONTEND_PORT=80

# ── Resource Limits (GCP e2-medium: 4GB RAM) ──
POSTGRES_MEMORY_LIMIT=1g
REDIS_MEMORY_LIMIT=512m
KAFKA_MEMORY_LIMIT=1g
KAFKA_HEAP_OPTS=-Xmx512m -Xms256m
BACKEND_MEMORY_LIMIT=1g
FRONTEND_MEMORY_LIMIT=256m

# ── Docker Images ──
# BACKEND_IMAGE=ghcr.io/santapong/nexus-backend:latest
# FRONTEND_IMAGE=ghcr.io/santapong/nexus-frontend:latest

# ── Temporal (enable with: docker compose --profile temporal up) ──
TEMPORAL_HOST=temporal:7233
TEMPORAL_NAMESPACE=nexus
TEMPORAL_TASK_QUEUE=nexus-tasks
TEMPORAL_MEMORY_LIMIT=512m

# ── LangFuse (optional) ──
LANGFUSE_HOST=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
