services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: prod
    volumes: []
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: prod
    ports:
      - "80:80"
    volumes: []
    restart: unless-stopped
